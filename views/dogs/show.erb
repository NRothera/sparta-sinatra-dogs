<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <div id="content">
    <h1 id="title"><%= @dog.title %></h1>
    <p><%= @dog.body %></p>
    <img src="<%= @dog.image %>" alt="">
    </div>

    <form action=" <%= @dog.id %>" method="POST">
      <button type="button" name="button"><a href='/dogs/<%= @dog.id %>/edit'>Edit</a></button>
      <input type="hidden" name="_method" value="DELETE">
      <input type="submit" name="name" value="Delete">
    </form>
  </body>
</html>
